# Public assets

Drop these two images here so the login/signup screens show the produce photo
on the left panel (until then a green gradient is shown):

- `auth-login.jpg`  — used by the Login page (`/login`)
- `auth-signup.jpg` — used by the Signup page (`/register`)

Recommended: portrait-ish, at least 800×1000px, JPG/WebP.
